'use client'; import { FormEvent,useState } from 'react'; import { useRouter } from 'next/navigation'; import { ArrowRight,LockKeyhole } from 'lucide-react'; import { api } from '@/lib/api';
export default function Login(){const r=useRouter(),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);try{const x:any=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});localStorage.setItem('token',x.token);localStorage.setItem('staff',JSON.stringify(x.staff));r.push(`/staff/${x.staff.role}`)}catch(e:any){setError(e.message);setLoading(false)}}return <main className="loginPage"><section className="loginArt"><div className="brand light"><span>IK</span> Indian Kitchen</div><div><p className="eyebrow">Behind every good meal</p><h1>A calm kitchen.<br/><em>A cared-for table.</em></h1></div></section><section className="loginForm"><form onSubmit={submit}><LockKeyhole/><p className="eyebrow red">Staff access</p><h2>Welcome back.</h2><p>Sign in to your branch workspace.</p><label>Email<input name="email" type="email" defaultValue="admin@indiankitchen.local" required/></label><label>Password<input name="password" type="password" defaultValue="Admin@123" required/></label>{error&&<p className="error">{error}</p>}<button disabled={loading}>{loading?'Signing in…':<>Sign in <ArrowRight/></>}</button></form></section></main>}
