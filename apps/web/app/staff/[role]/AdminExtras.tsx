'use client';
import { FormEvent,useEffect,useState } from 'react';import { BarChart3,Download,Plus,ShieldCheck } from 'lucide-react';import { API,api,money } from '@/lib/api';
export function AdvancedAnalytics({branchId}:{branchId:string}){const[data,setData]=useState<any>(),[range,setRange]=useState('monthly');useEffect(()=>{api(`/api/admin/analytics/overview?range=${range}${branchId?`&branchId=${branchId}`:''}`).then(setData)},[branchId,range]);if(!data)return <div className="adminLoading">Loading analytics…</div>;return <div className="advancedAnalytics"><div className="analyticsRange"><span>Performance period</span><select value={range} onChange={e=>setRange(e.target.value)}><option value="daily">Today</option><option value="weekly">7 days</option><option value="monthly">30 days</option></select></div><div className="metricGrid"><article><span>Revenue</span><b>{money(data.summary.revenue)}</b></article><article><span>Orders</span><b>{data.summary.orders}</b></article><article><span>Average order</span><b>{money(data.summary.average_order_value)}</b></article><article><span>Average preparation</span><b>{Number(data.preparation.average_preparation_minutes).toFixed(1)} min</b></article></div><div className="analyticsPanels"><section><h3>Top-selling items</h3>{data.topItems.map((x:any,i:number)=><div key={x.item_name}><b>{i+1}</b><span>{x.item_name}</span><em>{x.quantity} sold</em></div>)}</section><section><h3>Payment breakdown</h3>{data.paymentBreakdown.map((x:any)=><div key={x.payment_method}><b>{x.payment_method.toUpperCase()}</b><span>{x.orders} orders</span><em>{money(x.total)}</em></div>)}</section><section><h3>Peak hours</h3>{data.peakHours.slice(0,8).map((x:any)=><div key={x.hour_of_day}><b>{String(x.hour_of_day).padStart(2,'0')}:00</b><span>Customer traffic</span><em>{x.orders} orders</em></div>)}</section><section><h3>Branch comparison</h3>{data.branches.map((x:any)=><div key={x.id}><b>{x.name}</b><span>{x.orders} orders</span><em>{money(x.revenue)}</em></div>)}</section></div></div>}
export function BranchManager({branches,reload}:{branches:any[];reload:()=>Promise<void>}){const[open,setOpen]=useState(false);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const form:any=Object.fromEntries(new FormData(e.currentTarget));form.gst_percent=Number(form.gst_percent||5);await api('/api/admin/branches',{method:'POST',body:JSON.stringify(form)});setOpen(false);await reload()}async function toggle(branch:any){if(!confirm(`${branch.active?'Deactivate':'Activate'} ${branch.name}?`))return;await api(`/api/admin/branches/${branch.id}`,{method:'PATCH',body:JSON.stringify({active:!branch.active})});await reload()}return <div className="branchManager"><div className="adminToolbar"><div><b>{branches.length}</b><span>restaurant branches</span></div><button onClick={()=>setOpen(!open)}><Plus/> Add branch</button></div>{open&&<form className="branchForm" onSubmit={create}><input name="name" placeholder="Branch name" required/><input name="slug" placeholder="url-slug" required/><input name="address" placeholder="Address" required/><input name="phone" placeholder="Phone"/><input name="gst_number" placeholder="GST number"/><input name="gst_percent" type="number" defaultValue="5"/><button>Create branch</button></form>}<div className="branchGrid">{branches.map(branch=><article className={!branch.active?'inactive':''} key={branch.id}><span>{branch.active?'Active':'Inactive'}</span><h3>{branch.name}</h3><p>{branch.address}</p><small>GST {branch.gst_percent}% · {branch.phone||'No phone'}</small><button onClick={()=>toggle(branch)}>{branch.active?'Deactivate':'Activate'}</button></article>)}</div></div>}
export function ReportCenter({branchId}:{branchId:string}){const[from,setFrom]=useState(''),[to,setTo]=useState('');async function download(format:string){const url=`${API}/api/admin/reports/orders?format=${format}&branchId=${branchId}${from?`&from=${from}`:''}${to?`&to=${to}`:''}`;const response=await fetch(url,{headers:{Authorization:`Bearer ${localStorage.getItem('token')}`}}),blob=await response.blob(),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`orders-report.${format}`;a.click();URL.revokeObjectURL(a.href)}async function bulkQr(){const response=await fetch(`${API}/api/admin/tables/qr/bulk?branchId=${branchId}`,{headers:{Authorization:`Bearer ${localStorage.getItem('token')}`}}),blob=await response.blob(),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='table-qr-codes.pdf';a.click()}return <div className="reportCenter"><div><BarChart3/><h2>Export business records</h2><p>Choose an optional date range and download auditable order history.</p></div><section><label>From<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>To<input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><button onClick={()=>download('csv')}><Download/> Download CSV</button><button onClick={()=>download('pdf')}><Download/> Download PDF</button><button onClick={bulkQr}><Download/> Bulk table QR PDF</button></section></div>}
export function AuditLogView({branchId}:{branchId:string}){const[logs,setLogs]=useState<any[]>([]);useEffect(()=>{api(`/api/admin/audit-logs${branchId?`?branchId=${branchId}`:''}`).then(x=>setLogs(x as any[]))},[branchId]);return <div className="auditView"><header><ShieldCheck/><div><h2>System audit trail</h2><p>Immutable record of sensitive staff actions.</p></div></header>{logs.map(log=><article key={log.id}><div><b>{log.user_name||'System'}</b><small>{log.user_role||'system'} · {log.branch_name||'Global'}</small></div><span>{log.action}<small>{log.entity_type} · {log.entity_id||'—'}</small></span><time>{new Date(log.created_at).toLocaleString('en-IN')}</time></article>)}</div>}
